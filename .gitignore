# Dependencias
node_modules/
package-lock.json # Opcional: algunos prefieren subirlo, otros no. Recomendado subirlo para consistencia.
# Si usas yarn:
# yarn.lock

# Variables de entorno (IMPORTANTE: No subir claves privadas)
.env
.env.test
.env.production

# Logs y depuración
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# Archivos de sistema operativo
.DS_Store
Thumbs.db

# Build y distribución (si compilas)
dist/
build/

# Coverage de tests
coverage/